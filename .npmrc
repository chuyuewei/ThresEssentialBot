# .npmrc
# Note: sqlite3 native binary may fail on some Linux hosts.
# The bot uses an in-memory fallback if SQLite is unavailable.
# To enable persistent storage, ensure build tools are available
# and run: npm rebuild sqlite3 --build-from-source
node-options=--no-warnings